<Window x:Class="rebar_settings.Views.RebarSettingsWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:rebar_settings.Views"
        mc:Ignorable="d"
        Title="RebarSettingsWindowView" Height="450" Width="800">
    <!-- <Window.Resources> -->
    <!--     <ControlTemplate x:Key="ThumbControlTemplate"> -->
    <!--         <Ellipse Width="5" Height="5" Stroke="Orange" Fill="Orange"></Ellipse> -->
    <!--     </ControlTemplate> -->
    <!-- </Window.Resources> -->
    <Grid>
        <Canvas>
            <ItemsControl ItemsSource="{Binding Thumbs}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="5" Height="5" Stroke="Orange" Fill="Orange"></Ellipse>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </Grid>
</Window>